<nav id="sideNav">
    <!-- Mobile only: thin bar housing the toggle -->
    <div class="nav-mobile-bar">
        <button class="nav-toggle" aria-label="Menu">
            <svg
                width="20"
                height="14"
                viewBox="0 0 20 14"
                fill="currentColor"
                aria-hidden="true"
            >
                <rect y="0" width="20" height="2" rx="1"></rect>
                <rect y="6" width="20" height="2" rx="1"></rect>
                <rect y="12" width="20" height="2" rx="1"></rect>
            </svg>
        </button>
    </div>

    <ul class="nav-links">
        <li><a href="#about">À propos</a></li>
        <li><a href="#experience">Expériences</a></li>
        <li><a href="#formations">Formations</a></li>
        <li><a href="#certifications">Certifications</a></li>
        <li><a href="#projects">Projets</a></li>
        <li><a href="#skills">Compétences</a></li>
    </ul>
</nav>

<style>
    #sideNav {
        position: fixed;
        top: 0;
        left: 0;
        width: var(--sidebar-width);
        height: 100vh;
        background: linear-gradient(
            160deg,
            var(--primary-darker) 0%,
            var(--primary) 60%,
            var(--primary-dark) 100%
        );
        box-shadow: 4px 0 24px rgba(0, 0, 0, 0.2);
        display: flex;
        flex-direction: column;
        justify-content: center;
        z-index: 100;
        overflow-y: auto;
    }

    .nav-mobile-bar {
        display: none;
    }

    .nav-toggle {
        background: none;
        border: 1px solid rgba(255, 255, 255, 0.3);
        border-radius: var(--radius);
        color: rgba(255, 255, 255, 0.85);
        padding: 0.35rem 0.5rem;
        cursor: pointer;
        line-height: 0;
        transition:
            border-color var(--ease),
            color var(--ease);
    }
    .nav-toggle:hover {
        border-color: rgba(255, 255, 255, 0.7);
        color: var(--white);
    }

    .nav-links {
        list-style: none;
        padding: 0;
    }
    .nav-links li {
        margin: 0;
        border-bottom: 1px solid rgba(255, 255, 255, 0.08);
    }
    .nav-links li:first-child {
        border-top: 1px solid rgba(255, 255, 255, 0.08);
    }

    .nav-links a {
        display: block;
        padding: 0.8rem 1.5rem;
        font-family: var(--font-heading);
        font-weight: 700;
        font-size: 0.9rem;
        letter-spacing: 0.14rem;
        text-transform: uppercase;
        color: rgba(255, 255, 255, 0.85);
        text-decoration: none;
        position: relative;
        text-align: center;
        transition:
            color var(--ease),
            padding-left var(--ease);
    }
    .nav-links a::before {
        content: '';
        position: absolute;
        left: 0;
        top: 50%;
        transform: translateY(-50%);
        width: 3px;
        height: 0;
        background: rgba(255, 255, 255, 0.9);
        border-radius: 2px;
        transition: height var(--ease);
    }
    .nav-links a:hover,
    .nav-links a:global(.active) {
        color: var(--white);
        padding-left: 2.1rem;
    }
    .nav-links a:hover::before,
    .nav-links a:global(.active)::before {
        height: 1.1rem;
    }

    @media (max-width: 991px) {
        #sideNav {
            width: 100%;
            height: var(--navbar-height);
            overflow: hidden;
            justify-content: flex-start;
            flex-direction: column;
            transition: height 0.3s ease;
        }
        #sideNav:global(.open) {
            height: auto;
            overflow: visible;
        }

        .nav-mobile-bar {
            display: flex;
            align-items: center;
            justify-content: flex-end;
            height: var(--navbar-height);
            padding: 0 1rem;
            flex-shrink: 0;
        }

        .nav-links {
            display: none;
        }
        #sideNav:global(.open) .nav-links {
            display: block;
        }
    }
</style>