<nav id="sideNav">
    <!-- Mobile only: thin bar housing the toggle -->
    <div class="nav-mobile-bar">
        <button class="nav-theme-btn" id="theme-toggle" aria-label="Basculer le thème">
            <svg class="icon-sun" viewBox="0 0 24 24" fill="currentColor" width="1em" height="1em" aria-hidden="true">
                <path d="M12 7a5 5 0 1 0 0 10A5 5 0 0 0 12 7zm0-5a1 1 0 0 1 1 1v2a1 1 0 0 1-2 0V3a1 1 0 0 1 1-1zm0 16a1 1 0 0 1 1 1v2a1 1 0 0 1-2 0v-2a1 1 0 0 1 1-1zm9-9h-2a1 1 0 0 1 0-2h2a1 1 0 0 1 0 2zM5 12a1 1 0 0 1-1 1H2a1 1 0 0 1 0-2h2a1 1 0 0 1 1 1zm12.66-5.66a1 1 0 0 1 0 1.41l-1.42 1.42a1 1 0 1 1-1.41-1.42l1.41-1.41a1 1 0 0 1 1.42 0zM7.76 17.66l-1.41 1.41a1 1 0 1 1-1.42-1.41l1.42-1.42a1 1 0 0 1 1.41 1.42zm9.9 1.41a1 1 0 0 1-1.42 0l-1.41-1.41a1 1 0 1 1 1.41-1.42l1.42 1.42a1 1 0 0 1 0 1.41zM6.34 7.76 4.93 6.34a1 1 0 0 1 1.41-1.41l1.42 1.41A1 1 0 0 1 6.34 7.76z"/>
            </svg>
            <svg class="icon-moon" viewBox="0 0 24 24" fill="currentColor" width="1em" height="1em" aria-hidden="true">
                <path d="M12 3a9 9 0 1 0 9 9c0-.46-.04-.92-.1-1.36a5.389 5.389 0 0 1-4.4 2.26 5.403 5.403 0 0 1-3.14-9.8c-.44-.06-.9-.1-1.36-.1z"/>
            </svg>
        </button>
        <button class="nav-toggle" aria-label="Menu">
            <svg
                width="20"
                height="14"
                viewBox="0 0 20 14"
                fill="currentColor"
                aria-hidden="true"
            >
                <rect y="0" width="20" height="2" rx="1"></rect>
                <rect y="6" width="20" height="2" rx="1"></rect>
                <rect y="12" width="20" height="2" rx="1"></rect>
            </svg>
        </button>
    </div>

    <ul class="nav-links">
        <li><a href="#about">À propos</a></li>
        <li><a href="#experience">Expériences</a></li>
        <li><a href="#formations">Formations</a></li>
        <li><a href="#certifications">Certifications</a></li>
        <li><a href="#projects">Projets</a></li>
        <li><a href="#skills">Compétences</a></li>
    </ul>

    <!-- Desktop only: theme toggle + collapse button -->
    <div class="nav-footer">
        <button class="nav-theme-btn" id="theme-toggle-desktop" aria-label="Basculer le thème">
            <svg class="icon-sun" viewBox="0 0 24 24" fill="currentColor" width="1em" height="1em" aria-hidden="true">
                <path d="M12 7a5 5 0 1 0 0 10A5 5 0 0 0 12 7zm0-5a1 1 0 0 1 1 1v2a1 1 0 0 1-2 0V3a1 1 0 0 1 1-1zm0 16a1 1 0 0 1 1 1v2a1 1 0 0 1-2 0v-2a1 1 0 0 1 1-1zm9-9h-2a1 1 0 0 1 0-2h2a1 1 0 0 1 0 2zM5 12a1 1 0 0 1-1 1H2a1 1 0 0 1 0-2h2a1 1 0 0 1 1 1zm12.66-5.66a1 1 0 0 1 0 1.41l-1.42 1.42a1 1 0 1 1-1.41-1.42l1.41-1.41a1 1 0 0 1 1.42 0zM7.76 17.66l-1.41 1.41a1 1 0 1 1-1.42-1.41l1.42-1.42a1 1 0 0 1 1.41 1.42zm9.9 1.41a1 1 0 0 1-1.42 0l-1.41-1.41a1 1 0 1 1 1.41-1.42l1.42 1.42a1 1 0 0 1 0 1.41zM6.34 7.76 4.93 6.34a1 1 0 0 1 1.41-1.41l1.42 1.41A1 1 0 0 1 6.34 7.76z"/>
            </svg>
            <svg class="icon-moon" viewBox="0 0 24 24" fill="currentColor" width="1em" height="1em" aria-hidden="true">
                <path d="M12 3a9 9 0 1 0 9 9c0-.46-.04-.92-.1-1.36a5.389 5.389 0 0 1-4.4 2.26 5.403 5.403 0 0 1-3.14-9.8c-.44-.06-.9-.1-1.36-.1z"/>
            </svg>
        </button>
        <button class="nav-collapse-btn" aria-label="Réduire la navigation">
            <svg viewBox="0 0 24 24" fill="currentColor" width="1em" height="1em" aria-hidden="true">
                <path d="M15.41 7.41 14 6l-6 6 6 6 1.41-1.41L10.83 12z"/>
            </svg>
        </button>
    </div>
</nav>

<!-- Desktop only: floating tab to reopen the nav -->
<button class="nav-reopen-btn" aria-label="Ouvrir la navigation">
    <svg viewBox="0 0 24 24" fill="currentColor" width="1em" height="1em" aria-hidden="true">
        <path d="M8.59 16.59 10 18l6-6-6-6-1.41 1.41L13.17 12z"/>
    </svg>
</button>

<style>
    #sideNav {
        position: fixed;
        top: 0;
        left: 0;
        width: var(--sidebar-width);
        height: 100vh;
        background: linear-gradient(
            160deg,
            var(--primary-darker) 0%,
            var(--primary) 60%,
            var(--primary-dark) 100%
        );
        box-shadow: 4px 0 24px rgba(0, 0, 0, 0.2);
        display: flex;
        flex-direction: column;
        justify-content: center;
        z-index: 100;
        overflow-y: auto;
    }

    .nav-mobile-bar {
        display: none;
    }

    .nav-toggle {
        background: none;
        border: 1px solid rgba(255, 255, 255, 0.3);
        border-radius: var(--radius);
        color: rgba(255, 255, 255, 0.85);
        padding: 0.35rem 0.5rem;
        cursor: pointer;
        line-height: 0;
        transition:
            border-color var(--ease),
            color var(--ease);
    }
    .nav-toggle:hover {
        border-color: rgba(255, 255, 255, 0.7);
        color: var(--white);
    }

    .nav-links {
        list-style: none;
        padding: 0;
    }
    .nav-links li {
        margin: 0;
        border-bottom: 1px solid rgba(255, 255, 255, 0.08);
    }
    .nav-links li:first-child {
        border-top: 1px solid rgba(255, 255, 255, 0.08);
    }

    .nav-links a {
        display: block;
        padding: 0.8rem 1.5rem;
        font-family: var(--font-heading);
        font-weight: 700;
        font-size: 0.9rem;
        letter-spacing: 0.14rem;
        text-transform: uppercase;
        color: rgba(255, 255, 255, 0.85);
        text-decoration: none;
        position: relative;
        text-align: center;
        transition:
            color var(--ease),
            padding-left var(--ease);
    }
    .nav-links a::before {
        content: '';
        position: absolute;
        left: 0;
        top: 50%;
        transform: translateY(-50%);
        width: 3px;
        height: 0;
        background: rgba(255, 255, 255, 0.9);
        border-radius: 2px;
        transition: height var(--ease);
    }
    .nav-links a:hover,
    .nav-links a:global(.active) {
        color: var(--white);
        padding-left: 2.1rem;
    }
    .nav-links a:hover::before,
    .nav-links a:global(.active)::before {
        height: 1.1rem;
    }

    /* Desktop footer: theme toggle + collapse */
    .nav-footer {
        position: absolute;
        bottom: 1.25rem;
        left: 50%;
        transform: translateX(-50%);
        display: flex;
        gap: 0.5rem;
        align-items: center;
    }

    .nav-theme-btn,
    .nav-collapse-btn {
        display: flex;
        align-items: center;
        justify-content: center;
        background: rgba(255, 255, 255, 0.1);
        border: 1px solid rgba(255, 255, 255, 0.2);
        border-radius: var(--radius);
        color: rgba(255, 255, 255, 0.7);
        width: 2rem;
        height: 2rem;
        cursor: pointer;
        transition:
            background var(--ease),
            color var(--ease);
    }
    .nav-theme-btn:hover,
    .nav-collapse-btn:hover {
        background: rgba(255, 255, 255, 0.2);
        color: var(--white);
    }

    /* Sun shown in light mode, moon in dark */
    .icon-moon { display: none; }
    .icon-sun  { display: block; }
    :global(html.dark) .icon-moon { display: block; }
    :global(html.dark) .icon-sun  { display: none; }

    /* Floating reopen tab */
    .nav-reopen-btn {
        position: fixed;
        top: 50%;
        left: 0;
        transform: translateY(-50%);
        z-index: 101;
        background: var(--primary);
        border: none;
        border-radius: 0 var(--radius) var(--radius) 0;
        color: var(--white);
        width: 1.5rem;
        height: 3rem;
        cursor: pointer;
        align-items: center;
        justify-content: center;
        box-shadow: 2px 0 8px rgba(0, 0, 0, 0.2);
        transition: background var(--ease);
    }
    .nav-reopen-btn:hover {
        background: var(--primary-dark);
    }

    @media (max-width: 991px) {
        #sideNav {
            width: 100%;
            height: var(--navbar-height);
            overflow: hidden;
            justify-content: flex-start;
            flex-direction: column;
            transition: height 0.3s ease;
        }
        #sideNav:global(.open) {
            height: auto;
            overflow: visible;
        }

        .nav-mobile-bar {
            display: flex;
            align-items: center;
            justify-content: flex-end;
            gap: 0.5rem;
            height: var(--navbar-height);
            padding: 0 1rem;
            flex-shrink: 0;
        }

        .nav-links {
            display: none;
        }
        #sideNav:global(.open) .nav-links {
            display: block;
        }

        .nav-footer {
            display: none;
        }
    }
</style>

<script>
    const collapseBtn = document.querySelector('.nav-collapse-btn')!;
    const reopenBtn = document.querySelector('.nav-reopen-btn')!;
    const themeToggles = document.querySelectorAll('.nav-theme-btn');

    function setCollapsed(collapsed: boolean) {
        document.body.classList.toggle('nav-collapsed', collapsed);
        localStorage.setItem('navCollapsed', String(collapsed));
    }

    function toggleTheme() {
        const isDark = document.documentElement.classList.toggle('dark');
        localStorage.setItem('theme', isDark ? 'dark' : 'light');
    }

    if (localStorage.getItem('navCollapsed') === 'true') {
        document.body.classList.add('nav-collapsed');
    }

    collapseBtn.addEventListener('click', () => setCollapsed(true));
    reopenBtn.addEventListener('click', () => setCollapsed(false));
    themeToggles.forEach((btn) => btn.addEventListener('click', toggleTheme));
</script>